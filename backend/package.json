{
  "name": "uptime-backend",
  "version": "1.0.0",
  "description": "Uptime monitor con Cloud Scheduler + BigQuery + Firestore + Secret Manager",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch --env-file=.env index.js",
    "setup-bq": "node scripts/setup-bigquery.js"
  },
  "engines": { "node": ">=20.0.0" },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.0",
    "@google-cloud/firestore": "^7.10.0",
    "@google-cloud/secret-manager": "^5.6.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
